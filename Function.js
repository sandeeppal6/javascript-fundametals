function deposit(principle, depositAmount) {
  return principle + depositAmount;
}

const deposit1 = function (principle, depositAmount) {
  return principle + depositAmount;
};

const deposit2 = (principle, depositAmount) => {
  return principle + depositAmount;
};

const deposit3 = (principle, depositAmount) => principle + depositAmount;
